package LinerList.Interface;

public interface Node {
    //get the data segment of the node;
    public Object getData();

    //set the node using the object;
    public void setData(Object object);
}
